# Node modules should not be copied — they're installed inside the container
node_modules

# Environment variables
.env

# Ignore Git and related metadata
.git
.gitignore

# Docker files themselves (optional — only if not needed inside the image)
Dockerfile
docker-compose.yml

# Log files
*.log
